# Image Brief

Create a clean technical layer diagram for the header image.

Concept: `llmff` as the bounded execution layer between supervisors and runtime dependencies.

Structure:

- White or very light background.
- Three horizontal layers with thin dividers.
- Top layer: `Supervisor / agent host` with small labels `planning`, `memory`, `policy`, `retry decision`.
- Middle layer: `llmff` with labels `manifest execution`, `typed artifacts`, `trace/events`, `exit code`.
- Bottom layer: `Models`, `tools`, `files`, `plugins`.
- Draw a clear process boundary around the `llmff` layer.
- Use neutral colors and precise labels.

Avoid:

- Mascots.
- Decorative gradients.
- Marketing-style hero art.
- Generic robot imagery.
