# llmff v0.8.0 Release Candidate Evidence

`v0.8.0` is the first release candidate in the v1.0 API-freeze train.

## Tag

- Tag: `v0.8.0`
- Release commit: `95cd3ebf16cb6e0e6630fba29da183d47e55424f`
- GitHub Release: <https://github.com/syndicalt/llmff/releases/tag/v0.8.0>

## CI Evidence

- Release artifacts workflow: GitHub Actions run `26723385951`
- Result: success
- Evidence: release-tag CI built and smoked Linux, macOS, and Windows archive
  artifacts, generated Debian and Arch metadata artifacts where applicable,
  generated unsigned macOS and Windows native installer artifacts, generated the
  release trust manifest, created the GitHub Release, and uploaded release
  assets.

Main branch CI for the follow-up smoke-path fix is also green:

- Main CI workflow: GitHub Actions run `26723527584`
- Commit: `e4471d47ecb738623ce4905228f31d0caec12e09`
- Result: success
- Evidence: formatting, clippy, locked workspace tests, schema, plugin,
  governance, ecosystem, real-world workflow, and event-streaming gates passed.

## Post-Release Verification

Published release asset verification passed:

```bash
scripts/check-release-assets.sh v0.8.0
```

Observed result:

- all published asset SHA-256 checks passed;
- archive smoke reported `archive smoke succeeded`;
- Debian package smoke reported `deb smoke succeeded`;
- final verifier output reported `release asset verification succeeded for v0.8.0`.

GitHub install smoke passed:

```bash
scripts/smoke-install.sh --git https://github.com/syndicalt/llmff --tag v0.8.0
```

Observed result:

- installed `llmff v0.8.0` from
  `https://github.com/syndicalt/llmff?tag=v0.8.0#95cd3ebf`;
- `llmff --version` reported `llmff 0.8.0`;
- deterministic CLI smoke reported `run cli-run succeeded`.

## Notes

- Windows and macOS artifacts are unsigned for this release candidate.
- Package-manager publication remains parked; checked-in package-manager
  metadata is not v0.8.0 publication evidence.
- GitHub Actions reported Node.js 20 deprecation annotations for current
  `actions/*` usage and a Windows runner image redirection notice. These are
  follow-up maintenance items, not release-candidate blockers.
