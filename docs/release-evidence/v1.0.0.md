# llmff v1.0.0 Release Evidence

`v1.0.0` is the first stable release of the documented v1 contract.

## Tag

- Tag: `v1.0.0`
- Release commit: `18eb62a18d40935eb1fc0b07109ff0eba3807edb`
- GitHub Release: <https://github.com/syndicalt/llmff/releases/tag/v1.0.0>
- GitHub Release state: published, not draft, not prerelease

## CI Evidence

- Main CI workflow: GitHub Actions run `26723793883`
- Main CI commit: `18eb62a18d40935eb1fc0b07109ff0eba3807edb`
- Main CI result: success
- Evidence: formatting, clippy, locked workspace tests, schema, plugin,
  governance, ecosystem, real-world workflow, and event-streaming gates passed.

- Release artifacts workflow: GitHub Actions run `26723827131`
- Release artifacts result: success
- Evidence: release-tag CI built and smoked Linux, macOS, and Windows archive
  artifacts, generated Debian and Arch metadata artifacts where applicable,
  generated unsigned macOS and Windows native installer artifacts, generated the
  release trust manifest, created the GitHub Release, and uploaded release
  assets.

## Local Release Gate

The final local release gate passed before tagging:

```bash
cargo fmt --check
cargo clippy --workspace --all-targets -- -D warnings
cargo test --workspace --locked --no-fail-fast
python3 scripts/check-schema-contract.py
bash scripts/check-plugin-fixtures.sh
bash scripts/check-governance-readiness.sh
bash scripts/check-ecosystem-readiness.sh
bash scripts/check-real-world-workflows.sh
LLMFF_BIN=target/debug/llmff bash scripts/smoke-events-streaming.sh
scripts/release-preflight.sh v1.0.0
```

The dependency and security review is recorded in
[`docs/release-evidence/v1.0.0-security.md`](v1.0.0-security.md).

## Post-Release Verification

Published release asset verification passed:

```bash
scripts/check-release-assets.sh v1.0.0
```

Observed result:

- all published asset SHA-256 checks passed;
- archive smoke reported `archive smoke succeeded`;
- Debian package smoke reported `deb smoke succeeded`;
- final verifier output reported `release asset verification succeeded for v1.0.0`.

GitHub install smoke passed:

```bash
scripts/smoke-install.sh --git https://github.com/syndicalt/llmff --tag v1.0.0
```

Observed result:

- installed `llmff v1.0.0` from
  `https://github.com/syndicalt/llmff?tag=v1.0.0#18eb62a1`;
- `llmff --version` reported `llmff 1.0.0`;
- deterministic CLI smoke reported `run cli-run succeeded`.

## Notes

- Windows and macOS artifacts are unsigned for this release.
- Package-manager publication remains parked; checked-in package-manager
  metadata is not v1.0.0 publication evidence.
- GitHub Actions reported Node.js 20 deprecation annotations for current
  `actions/*` usage and a Windows runner image redirection notice. These are
  follow-up maintenance items, not v1.0.0 release blockers.
