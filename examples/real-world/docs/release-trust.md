Production workflows should prefer immutable GitHub Release assets with
checksums. Package-manager publication is a support commitment, not only a
metadata generation task. Unsigned Windows and macOS installers should be
described honestly until paid signing and notarization credentials exist.
